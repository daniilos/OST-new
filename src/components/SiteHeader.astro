---
const navItems = [
    { label: 'Услуги', href: '#services' },
    { label: 'Почему OST', href: '#why-us' },
    { label: 'Сравнение', href: '#comparison' },
    { label: 'Отзывы', href: '#testimonials' },
    { label: 'Контакты', href: '#contact' }
];
---

<header class="sticky top-0 z-50 border-b border-border bg-white/95 backdrop-blur supports-[backdrop-filter]:bg-white/90">
    <div class="container-main flex items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
        <a href="/" class="inline-block no-underline transition-transform duration-200 hover:scale-105">
            <span class="text-3xl font-extrabold tracking-tight text-[#1a3a6b]">OST</span>
        </a>

        <nav class="hidden items-center gap-1 md:flex" aria-label="Main navigation">
            {navItems.map((item) => (
                <a
                    href={item.href}
                    class="px-3 py-2 text-sm font-medium text-card-foreground no-underline transition-all duration-200 hover:text-[#2563eb] hover:underline hover:underline-offset-4 hover:decoration-[#2563eb]"
                >
                    {item.label}
                </a>
            ))}
        </nav>

        <div class="flex items-center gap-3">
            <a href="tel:+380442899541" class="hidden text-sm font-semibold text-[#1a3a6b] no-underline lg:inline">
                +38 (044) 289 95 41
            </a>
            <a href="#contact" class="button-primary hidden sm:inline-flex">Бесплатная консультация</a>
            <button
                id="mobile-menu-btn"
                class="flex h-10 w-10 items-center justify-center rounded-lg text-foreground transition-colors hover:bg-[#eff4ff] md:hidden"
                aria-label="Открыть меню"
                aria-expanded="false"
            >
                <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                </svg>
            </button>
        </div>
    </div>

    <div id="mobile-menu" class="hidden border-t border-border bg-white md:hidden">
        <nav class="container-main flex flex-col gap-1 px-4 py-4" aria-label="Mobile navigation">
            {navItems.map((item) => (
                <a
                    href={item.href}
                    class="mobile-nav-link rounded-md px-3 py-2.5 text-sm font-medium text-card-foreground no-underline transition-colors hover:bg-[#eff4ff] hover:text-[#1a3a6b]"
                >
                    {item.label}
                </a>
            ))}
            <a href="#contact" class="button-primary mt-2 text-center">Бесплатная консультация</a>
        </nav>
    </div>
</header>

<script>
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    btn?.addEventListener('click', () => {
        const isOpen = !menu?.classList.contains('hidden');
        menu?.classList.toggle('hidden');
        btn.setAttribute('aria-expanded', String(!isOpen));
    });
    document.querySelectorAll('.mobile-nav-link').forEach((link) => {
        link.addEventListener('click', () => {
            menu?.classList.add('hidden');
            btn?.setAttribute('aria-expanded', 'false');
        });
    });
</script>
