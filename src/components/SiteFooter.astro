---
const navGroups = [
    {
        title: 'Послуги',
        links: [
            { label: 'IT-аутсорсинг', href: '#services' },
            { label: 'Обслуговування ПК', href: '#services' },
            { label: 'Сервери', href: '#services' },
            { label: 'Мережі', href: '#services' },
            { label: 'Кібербезпека', href: '#services' }
        ]
    },
    {
        title: 'Компанія',
        links: [
            { label: 'Чому OST', href: '#why-us' },
            { label: 'Порівняння', href: '#comparison' },
            { label: 'Відгуки', href: '#testimonials' },
            { label: 'Контакти', href: '#contact' }
        ]
    }
];
---

<footer class="border-t border-[#dbe5ff] bg-[#1a3a6b] text-white">
    <div class="container-main section-padding !py-12">
        <div class="grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
            <div class="lg:col-span-2">
                <a href="/" class="mb-4 inline-block no-underline">
                    <img src="/images/ostnew1.svg" alt="OST" class="h-9 w-auto brightness-0 invert" width="443" height="443" loading="lazy" decoding="async" />
                </a>
                <p class="mt-3 max-w-sm text-sm leading-relaxed text-white/75">
                    Корпоративний IT-аутсорсинг для малого та середнього бізнесу в Києві:
                    підтримка робочих місць, серверів, мереж та інформаційної безпеки.
                </p>
            </div>

            {navGroups.map((group) => (
                <div>
                    <h4 class="mb-4 text-sm font-semibold uppercase tracking-wider text-white/55">{group.title}</h4>
                    <ul class="flex flex-col gap-1">
                        {group.links.map((link) => (
                            <li>
                                <a href={link.href} class="inline-block py-1.5 text-sm text-white/75 no-underline transition-all duration-200 hover:translate-x-1 hover:text-white">
                                    {link.label}
                                </a>
                            </li>
                        ))}
                    </ul>
                </div>
            ))}
        </div>

        <div class="mt-10 flex flex-col items-center gap-2 border-t border-white/15 pt-6 sm:flex-row sm:justify-between">
            <p class="text-sm text-white/55">
                &copy; 1995-{new Date().getFullYear()} OST. IT-аутсорсинг для бізнесу в Києві.
            </p>
            <p class="text-sm text-white/45">
                Корпоративна IT-підтримка під ключ.
            </p>
        </div>
    </div>
</footer>
